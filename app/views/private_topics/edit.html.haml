= form_for @topic, url: private_topic_path(@topic) do |f|
  %div
    = f.label :title
    = f.text_field :title

  %div
    = f.label :content
    %br
    = f.text_area :content

  %div
    = f.submit "Mettre à jour la discussion"
