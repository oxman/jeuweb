%h2
  Éditer votre réponse à
  %q= @topic.title

= form_for @reply, url: private_topic_reply_path(@topic, @reply) do |f|
  %div
    = f.label :content
    %br
    = f.text_area :content

  %div
    = f.submit "Modifier votre réponse"
